#include<stdio.h>
int main()
{
    int t;
    scanf("%d", &t);
    while(t--)
    {
        int s;
        scanf("%d", &s);
        char str[s+1];
        scanf("%s", str);
        // printf("%s\n", str);
        if(s % 2 == 0)
        {
            
        }
    }
    return 0;
}